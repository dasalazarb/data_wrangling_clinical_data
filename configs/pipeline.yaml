project_name: sjogren_clinical_data_pipeline
run_name_prefix: sjogren_pipeline
paths:
  raw_dir: data/raw
  staging_dir: data/staging
  curated_dir: data/curated
  analytic_dir: data/analytic
  excluded_dir: data/excluded
  reports_dir: reports
  logs_dir: logs
settings:
  fail_fast: false
  log_level: INFO

cohort_harmonization:
  enabled: true
  patient_id_column: patient_id
  once_source_path: data/raw/CTDB Data Download 11D.xlsx
  quince_source_path: data/raw/CTDB Data Download 15D.xlsx
  once_sheet_name: 0
  quince_sheet_name: 0
  codebook_path: data/input_catalog/codebook_final_harmonized_once_quince.xlsx
  codebook_sheet: final_codebook
  question_name_column: QUESTION_NAME
  needs_review_column: needs_review

datasets: []
